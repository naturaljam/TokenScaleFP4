# SPDX-License-Identifier: Apache-2.0

name: CI

on:
  push:
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install -e ".[dev,gpu]"
      - run: python -m ruff check .
      - run: npx --yes pyright@1.1.411 src
      - run: python -m pytest -m "not sm120" -q
      - run: python scripts/audit_public_repo.py --tracked
